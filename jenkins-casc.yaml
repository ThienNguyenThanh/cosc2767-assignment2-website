jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
       - id: "admin"
         password: "admin123"
  authorizationStrategy: loggedInUsersCanDoAnything
tool:
  maven:
    installations:
      - name: "Maven 3"
        home: /opt/maven
  git:
    installations:
      - name: Git
        home: "git"

  jdk:
    installations:
      - name: "JDK 11"
        home: /usr/lib/jvm/java-11-openjdk-amd64


jobs:
  - script: |
      job('example 1'){
          scm{
              git {
                  remote {
                      url('https://github.com/ThienNguyenThanh/cosc2767-assignment2-website.git')
                  }
                  branch('main')
              }
          }
          steps {
              maven('pom.xml')
              maven {
                  goals('clean')
                  goals('install')
              }
              publishOverSsh {
                  server('docker-server') {
                      credentials ('dockeradmin') {
                      
                      }
                      label ('44.204.185.88')
                      transferSet {
                          sourceFiles('target/*.war')
                          removePrefix('target')
                      }
                  }
              }
          }
          
      }
